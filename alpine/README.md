# Advanced `alpine` Image

This container image is meant to provide a toolbox to perform basic Linux interactions. It is based on alpine and adds the following dependencies:
- curl
- jq
- yq
- gettext-base (e.g. envsubst)
- grep
- bash
- git
- openssl
